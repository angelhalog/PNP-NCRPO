import 'package:flutter/material.dart';

class InfoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('EDPPAS'),
      ),
      body: Center(
        child: Text('This is the app EDPPAS redirection screen.'),
      ),
    );
  }
}
