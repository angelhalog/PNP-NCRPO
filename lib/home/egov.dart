import 'package:flutter/material.dart';

class InfoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('EGOV PH'),
      ),
      body: Center(
        child: Text('This is the app EGOV PH redirection screen.'),
      ),
    );
  }
}
